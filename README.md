# 住宅購入安心度シミュレーター

住宅購入と賃貸継続を、生涯収支・手元の金融資産・住宅純資産から比較する静的Webアプリです。

## GitHub Pagesで公開する

1. このフォルダの中身をGitHubリポジトリへアップロードします。
2. リポジトリの `Settings` → `Pages` を開きます。
3. `Source` で `GitHub Actions` を選択します。
4. `main` ブランチへ反映すると、公開処理が始まります。

入力内容はブラウザ内だけで計算され、サーバーへ送信されません。
